# ============================================================================
# Sortify Frontend - Environment Configuration Template
# ============================================================================
# 使用說明：
# 1. 複製此文件為 .env: cp .env.example .env
# 2. 填入實際的配置值
# 3. 不要將 .env 文件提交到版本控制系統
# 4. Create React App 只會載入以 REACT_APP_ 開頭的環境變數
# ============================================================================

# ============================================================================
# API 配置
# ============================================================================

# 後端 API 基礎 URL
# 開發環境：使用相對路徑 /api/v1（通過 setupProxy.js 代理到 http://localhost:8000）
# 生產環境：設置為完整的後端 API URL
# 注意：留空或使用 /api/v1 時，會使用 setupProxy.js 的代理配置
REACT_APP_API_BASE_URL=/api/v1

# 替代配置名稱（向後兼容）
# REACT_APP_API_URL=/api/v1

# ============================================================================
# Google OAuth 配置
# ============================================================================

# Google OAuth 客戶端 ID（用於 Google 登入功能）
# 獲取方式：
# 1. 前往 Google Cloud Console: https://console.cloud.google.com
# 2. 創建 OAuth 2.0 客戶端 ID
# 3. 授權的 JavaScript 來源: http://localhost:3000
# 4. 授權的重定向 URI: http://localhost:3000/auth/gmail-callback
# 5. 複製客戶端 ID 到下方
# 注意：如果不配置此項，Google 登入功能將被禁用
REACT_APP_GOOGLE_CLIENT_ID=

# ============================================================================
# 部署配置（可選）
# ============================================================================

# 生產環境示例配置：
# REACT_APP_API_BASE_URL=https://your-backend-api.com/api/v1
# REACT_APP_GOOGLE_CLIENT_ID=your-production-google-client-id

# ============================================================================
# 開發環境說明
# ============================================================================
# 
# 開發模式下的代理配置：
# - 前端運行於: http://localhost:3000
# - 後端運行於: http://localhost:8000
# - setupProxy.js 會將 /api/* 請求代理到 http://localhost:8000/api/*
# - 因此 REACT_APP_API_BASE_URL 可以使用相對路徑 /api/v1
#
# 如果後端運行在不同的端口或域名，請修改：
# - setupProxy.js 中的 target 配置
# - 或直接設置 REACT_APP_API_BASE_URL 為完整的 URL
#
# ============================================================================
